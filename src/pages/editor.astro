---
import Layout from '../layouts/Layout.astro';
import EmojiEditor from '../components/editor/EmojiEditor';
import { withBase } from '../lib/url';
import '../styles/editor.css';
import '../styles/editor-gif.css';

const base = import.meta.env.BASE_URL;
const homeUrl = withBase('/', base);
const browseUrl = withBase('/emojis', base);
---
<Layout
  title="Emoji editor — Crop, resize and edit animated GIF emoji"
  description="Edit emoji directly in your browser. Crop, resize, rotate, flip, change GIF animation speed, scrub frames, add padding or a background, then export PNG, WebP or animated GIF without uploading the image to a server."
  keywords={['emoji editor', 'GIF emoji editor', 'animated emoji', 'emoji crop', 'emoji resize', 'emoji maker', 'PNG emoji', 'WebP emoji']}
  breadcrumbs={[
    { name: 'Home', href: homeUrl },
    { name: 'Emoji editor' }
  ]}
>
  <section class="shell">
    <EmojiEditor browseUrl={browseUrl} client:only="react" />
  </section>
</Layout>