import { Button } from '../motion/button';

export default function HomeSearchButton() {
  return (
    <Button type="submit" size="lg" className="beui-search-button">
      Search
    </Button>
  );
}
